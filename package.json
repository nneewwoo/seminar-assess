{
  "name": "root",
  "version": "0.0.0",
  "packageManager": "bun@1.1.38",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev:api": "turbo watch --filter @seminar-assess/api dev",
    "build": "turbo run build --no-daemon",
    "lint": "biome lint --write .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "^1.1.14",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
