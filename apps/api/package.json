{
  "name": "@seminar-assess/api",
  "version": "0.1.0",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build:api": "bun build src/index.ts --target bun --minify --outdir ./dist",
    "start": "bun dist/index.js"
  },
  "dependencies": {
    "@seminar-assess/db": "workspace:*",
    "hono": "^4.6.14"
  }
}
